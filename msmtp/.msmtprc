account default
auth on
tls on
# don't use starttls due to its many security related bugs
#tls_starttls on
# use the system trust store (requires SUSE certs to be added there, too)
#tls_trust_file /etc/ssl/certs/ca-certificates.crt
logfile ~/.msmtp.log
auth on
user pperego

from pperego@suse.de
user pperego@mail
host imap.suse.de
port 587
#password 
#passwordeval /usr/bin/pass email/imap.suse.de
